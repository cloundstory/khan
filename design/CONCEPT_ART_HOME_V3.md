# Concept art home v3

Generated 2026-09-10 with the built-in imagegen skill from the three references in `art direction/`:

- desktop app composition reference
- mobile app composition reference
- isolated hand-drawn armchair reference

Output: `public/concept/home-v3.png`.

The image is a visual direction plate only. It intentionally contains no readable UI copy or controls. The runtime mockup places DOM labels and interaction hotspots over this plate, then opens a real React Three Fiber book viewer when a hotspot is selected.

This is a concept image, so the visible books are illustrative placeholders. Production Home must replace them with `Book.id`-bound book layers and real cover data.
